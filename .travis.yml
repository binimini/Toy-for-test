language: java
jdk:
  - openjdk11

branches:
  only:
    - build
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'
before_install:
  - chomod +x gradlew
script: "./gradlew clean build"